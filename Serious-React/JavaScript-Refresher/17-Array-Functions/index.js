// Array Functions

const arr = ['Sports', 'Reading', 'Cooking'];

const arrMap = arr.map(item => item);
console.log(arrMap);

const arrFind = arr.find(item => item == 'Reading');
console.log(arrFind);